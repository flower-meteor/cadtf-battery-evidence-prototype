# CADTF 可运行原型完成说明

## 公网访问

任何手机或电脑均可直接打开：

https://flower-meteor.github.io/cadtf-battery-evidence-prototype/

公网版不需要本地脚本，数据与计算逻辑直接在浏览器中运行。

公网版包含六步引导演示、测量时序图、模型适用域图、CSV/JSON/Excel
数据导入、角色权限、本地审计轨迹、案例分享、模型卡、公式和参考文献，
并支持手机主屏幕安装与离线缓存。

## 一、已经完成什么

本目录中的原型已经实现从电池装机到维修证据审查的完整软件流程：

`装机记录 -> 测量导入 -> 数据校验 -> 模型适用域检查 -> 健康估计 -> 证据包哈希 -> 人工审查 -> 导出`

具体包括：

- 飞机、电池包、模型版本和装机记录管理；
- 构型修订、软件版本和校准状态变更；
- 手工测量录入和 CSV 批量导入；
- 容量、内阻、温度、充放电倍率、循环次数检查；
- SOH、容量健康、内阻健康、可用功率和不确定度估计；
- `valid`、`review_required`、`blocked` 三态安全边界；
- 每次估计自动生成不可变 JSON 证据包及 SHA-256 哈希；
- 阻断结果不能直接接受，只能拒收、补数据或升级审查；
- 合成试点对照评价界面；
- 中英双语证据字典；
- Excel、CSV、JSON 和 SQLite 导出；
- 13 项自动化测试；
- 独立证据校验和验证报告生成。

## 二、如何启动

双击：

`start_app.cmd`

浏览器打开：

`http://localhost:8501`

完整复核可双击：

`run_validation.cmd`

环境检查可双击：

`check_environment.cmd`

## 三、三个演示场景

### BAT-001

正常 NMC 电池包，温度和倍率均在模型域内。预期状态为 `valid`，SOH 为 93.8%，证据完整度为 100%。

### BAT-002

高应力 NMC 电池包，温度、倍率和循环次数超过验证域。预期状态为 `blocked`，系统会抑制维修建议并要求授权审查。

### BAT-003

LFP 电池包，使用独立 LFP 模型域。预期状态为 `valid`，SOH 为 95.4%。

## 四、主要交付文件

- `app.py`：网页原型入口。
- `cadtf\`：数据库、领域逻辑、健康估计、证据包和服务层。
- `tests\`：自动化测试。
- `data\cadtf_demo.db`：可运行的 SQLite 数据库。
- `exports\cadtf_prototype_export.xlsx`：全部数据表。
- `exports\evidence\`：三份独立证据包 JSON。
- `exports\evidence_manifest.csv`：证据包哈希和校验结果。
- `docs\PROTOTYPE_DESIGN.md`：系统设计说明。
- `docs\VALIDATION_REPORT.md`：当前验证报告。
- `DELIVERABLE_MANIFEST.csv`：所有交付文件的 SHA-256 清单。

## 五、验证结果

- 自动化测试：13 项通过，0 失败。
- 证据包：3 份全部通过哈希校验。
- 验证域：2 个有效，1 个按设计阻断。
- Streamlit 服务：健康检查返回 HTTP 200。
- 所有六个界面页面：均可正常渲染。
- 数据：全部为合成演示数据，不含隐私信息。

## 六、边界

本原型证明的是软件流程、证据组织和模型边界控制。健康估计仍然使用可解释的演示基线，尚未使用代表性电池包试验数据完成工程验证。

它不能用于：

- 适航批准；
- 维修放行；
- 飞行决策；
- 证明电池包安全性；
- 替代批准的维修程序。

真实试点还需要数据访问授权、实验室测试、独立模型确认、身份权限、签署机制、安全评审和监管沟通。
